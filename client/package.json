{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:ts": "tsc -w",
    "dev:css": "postcss source/**/*.postcss --base source --ext .css --dir public/dist --watch",
    "build": "npm run build:ts && npm run build:css",
    "build:ts": "tsc --build",
    "build:css": "postcss source/**/*.postcss --base source --ext .css --dir public/dist",
    "postupdate": "sh ./script/postinstall.sh"
  },
  "author": "Findoss",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "cssnano": "^4.1.10",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "postcss-import": "^14.0.0",
    "postcss-load-config": "^3.0.0",
    "postcss-nested": "^5.0.3",
    "stylelint-config-recommended": "^3.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "handlebars": "^4.7.6"
  }
}
